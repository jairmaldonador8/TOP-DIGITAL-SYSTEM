import { SitioConFondo } from '@/app/ideas/_piezas/fondo-unico'

/**
 * FONDO B · TINTA (#0d0b10).
 *
 * El casi-negro con tinte violeta que ya usa el sistema de color del
 * proyecto, ahora parejo en todo el sitio. Es el más cercano a lo que
 * hay hoy: mantiene la calidez de marca sin las manchas ni la costura.
 */
export default function FondoTinta() {
  return <SitioConFondo color="#0d0b10" />
}
